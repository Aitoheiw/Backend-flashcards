{
  "name": "koreanflashcard-backend",
  "version": "1.0.0",
  "description": "API JSON Server pour les flashcards coréennes",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3004 --host 0.0.0.0 --no-static"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "cors": "^2.8.5"
  }
}
